apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellyfin-web-route
spec:
  parentRefs:
  - name: main-gateway
    namespace: security
  hostnames:
  - "jellyfin.alstrom.home.arpa"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: jellyfin
      port: 8096