apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prowlarr-web-route
spec:
  parentRefs:
  - name: main-gateway
    namespace: security
  hostnames:
  - "prowlarr.alstrom.home.arpa"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: prowlarr
      port: 8989